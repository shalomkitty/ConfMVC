@model ConfDomain.Model.Publication
@{
    ViewData["Title"] = "Publication Details";
}

<section class="user-list-section">
    <div class="mb-4">
        <h1 class="mb-1">@ViewData["Title"]</h1>
    </div>

    <dl class="row">
        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Title)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.Title)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.UploadDate)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.UploadDate)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.User)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.User.FullName)</dd>
    </dl>

    <div class="mt-3">
        <a asp-action="Edit"
           asp-route-id="@Model.Id"
           class="btn btn-outline-user me-2">
            Edit
        </a>
        <a asp-action="Index" class="btn btn-outline-user">
            Back to List
        </a>
    </div>
</section>
